version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./backend/uploads:/app/backend/uploads   # persistir videos
      - ./backend/database.sqlite:/app/backend/database.sqlite  # persistir BD